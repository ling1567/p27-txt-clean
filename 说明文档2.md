本项目旨在开发一个基于 Web 界面的文本处理工具，专门用于清理、修复及标准化电子书（特别是 .txt 格式）的内容，提升阅读体验及兼容性。

# 0.用户的first look：
一个网页，左边2/3是功能区，右边1/3是txt内容预览区
浅色配色。

  +-----------------------------------------------------------------------+
  |  27 TXT Formatter - 文本清理工具                                        |
  +---------------------------------------+-------------------------------+
  | [ 📂 上传文件 (Max 50MB) ]             | [ 📄 文本预览区 (前500行) ]     |
  |                                                          |                               |
  | 💡 状态提示:                                    | 001: 第一章 启程               |
  |                                                         | 002:                           |
                             | 003: 这是一个漫长的夜晚...      |
  |                                                   | 004: 窗外的雨滴答滴答地响着。   |
  +---------------------------------------+ 005:                           |
  | ⚙️ 自动处理 (Automatic)                |                                 |
  | [x] 换行符归一化 (LF)                  |                               |
  | [ ] HTML 标签清理                     |                               |
  | [ ] 非法/控制字符过滤                 |                               |
  | [ ] 全半角转换 (数字/字母/标点)        |                               |
  | [ ] 段落清洗 (首尾去杂/自动缩进)       |                               |
  | [ ] 空行压缩 (自定义阈值)              |                               |
  | [ ] 断句缝合 (修复非正常换行)          |                               |
  | [🔒] 去除广告 (暂不支持)               |                               |
  +---------------------------------------+                               |
  | 🛠️ 手动处理 (Manual)                   |                               |
  | [ ] 手动过滤非法字符 [ 点击弹出 ]      |                               |
  | [ ] 章节识别与序号重排                 |                               |
  |                                       |    
   +---------------------------------------+ |                           |
  |        [ 🚀 开始处理 ]                 |                               |
  +---------------------------------------+                               |
  | [ 💾 保存处理后的文件 ]                |                               |
  +---------------------------------------+                               |
  | ▼ 日志信息 (Log)                       |                               |
  | [12:00:01] INFO: 文件上传成功          |                               |
  | [12:00:05] WARN: 发现 3 处疑似断句异常  |                               |


# 1.用户动作-上传txt文件（仅限txt格式，50M以内）
- 左边功能区的最上方是[上传文件]按钮
- 文件上传后，在预览区展示前500行的内容，预览区右边有滑动条
- 程序检测txt文件的编码格式，把文件改成utf-8编码。log新增提示：“已将xxx编码改成utf-8编码”
- BOM 去除： 自动探测并截断文件开头的字节顺序标记（如 UTF-8 的 `EF BB BF` 或 UTF-16 的 `FF FE`），确保输出纯净的无 BOM 文本，防止阅读器解析首行异常。log新增提示：“已去除BOM”

# 2.用户动作-根据需求选择需要清理的部分

左边功能区的中间是2个frame框（自动处理、手动处理）和[开始处理]按钮。
用户勾选需要的功能，然后点击[开始处理]按钮。
log新增提示：“已完成xxx功能”（每个勾选的功能是一条单独的log提示）
处理后右边预览区**刷新**，展示处理后的前500行（此时文档在内存里）。
log新增提示：“预览区已刷新为处理后的文件”
如果用户不满意，可以重新勾选功能，然后再次点击[开始处理]按钮，程序会重新处理。
#### 自动处理frame内包括
- **[ ]  换行符归一化**
- **[ ]  HTML 清理**
- **[ ] 非法字符过滤**
- **[ ]  全半角转换**
- **[ ]  段落清洗**
- **[ ]  空行压缩**
- **[ ]  断句缝合**
- [锁死状态] 去广告 （后面一行小字：“暂不支持该功能”）
#### 手动处理frame包括
- **[ ] 手动过滤非法字符**
- **[ ] 章节识别**

#### 对应的后端功能
* **1.换行符归一化（默认勾选）**：
    * **最高优先级**：在所有处理步骤的第一步，强制将 Windows 风格的 `\r\n` 和旧版 Mac 风格的 `\r` 统一替换为标准 Unix 换行符 `\n`。这能确保后续所有使用 `^` 和 `$` 的正则表达式行为一致。
* **2.HTML 清理：
    * **标签删除**：通过正则或解析树，一键清除文本中嵌套的 HTML/XML 结构标签（如 `<div>`, `<br/>`, `<span>` 等）。
    * **实体反转义**：将 HTML 实体安全还原为普通字符（例如将 `&nbsp;` 还原为空格，将 `&amp;` 还原为 `&`），保证正文内容的完整性。
    
*   **3.非法字符过滤**：
    *  **安全控制符剥离**：使用正则全局剔除除换行（`\n`, `\r`）和制表（`\t`）以外的所有 C0/C1 控制字符（涵盖 `U+0000` 至 `U+001F`，如 `U+0008` 退格, `U+001A` EOF 等）。  
    * **直接剔除控制字符**： `U+0008`, `U+000B`, `U+000C`, `U+001A` 等。
    *   **清除不可见零宽字符**：`U+200B`, `U+200C`, `U+200D`，`U+FEFF`。
    *   **空白符转换**：将 `U+00A0` 转换为标准半角空格`U+0020`。
    *   **乱码残留处理**：自动剔除解码失败产生的 `U+FFFD`。
    
*   **4.手动过滤非法字符**：（基于 Unicode 识别的交互式清理界面）
	* **UI 唤起**：在左侧面板提供一个【手动过滤乱码】的按钮，点击唤出模态弹窗（Modal）。
    *   **异常捕获输入**：支持用户在 UI 界面输入框（一个新的弹框）粘贴包含异常或干扰字符的文本原文（ 200 字以内）。
    *   **字符编码映射**：点击“确定”后，程序自动解析输入片段中的每个字符及其对应的 Unicode 编码（例如：'这' -> `U+8FD9`）。
    *   **可视化交互按钮**：界面将每个字符及其对应的编码生成独立的交互按钮（例如：`[我]` `[是]` `[&!]` `[小]` `[猫]` `[。]`），便于识别不可见字符或乱码。
    *  **不可见字符兜底展示**：若解析出纯粹的控制符或零宽符，按钮表面强制显示其十六进制特征码（如 `[ \u200b U+200B ]`），并在视觉上用特殊颜色高亮，提示用户这就是“隐藏的罪魁祸首”。
    *   **精准剔除逻辑**：用户点击需要删除的字符按钮（如 `[&!]`），程序将该字符对应的 Unicode 编码定义为推入全局状态树 `RuleConfig.base_cleaning.manual_blacklists` 数组中。后续在处理全量文本或动态切片时，该名单内的字符将被全局替换为空字符串 `''`。
    
* **5.全半角转换 ：
    * **精细化控制选项**：抛弃一刀切的转换，提供分类独立切换的配置项：
        1. **字母与数字 (Alphanumeric)**：默认执行**全角转半角**（例如将 `ＡＩ１２３` 转换为 `AI123`），保持英文和数据的紧凑感。
        2. **标点符号 (Punctuation)**：默认执行**半角转全角**（例如将 `, . ? !` 转换为 `， 。 ？ ！`），符合中文阅读习惯。
    * **安全边界保护**：在进行半角转全角标点时，程序需包含基本的豁免逻辑（例如：不处理连续的英文字母内部的标点，以防误伤 URL 网址或英文缩写）。

* **6.段落清洗**：
    * **首尾去杂 (Trim)**：逐行扫描，强制移除每一行开头和结尾的多余空白字符（包括半角空格 `U+0020`、全角空格 `U+3000` 以及制表符 `\t`）。
    * **标准中文缩进 (可选扩展)**：在首尾去杂之后，提供“强制段首缩进”开关，若开启，则在每个非空行的行首自动添加两个全角空格（`\u3000\u3000`）。

* **7.空行压缩 **：
    * **伪空行识别**：定义“空行”不仅包含真正的零字符行（`\n\n`），还包含仅有空格或制表符组成的“伪空行”。处理前需先将伪空行降维成真正的空行。
    * **阈值压缩逻辑**：允许用户自定义保留的空行阈值 N（默认配置 N=2）。程序将连续出现超过 N 个的空行，统一下压折叠为准确的 N 个空行。（若 N=0，则实现“紧凑排版”，消除段落间的所有空隙）。

* **8.章节识别**
	* **内置通用规则库 (Built-in Heuristics)**：
	    * **多形态数字兼容**：默认支持识别“第X章”、“第X节”、“第X回”、“Chapter X”等高频范式。底层的正则匹配必须同时兼容**阿拉伯数字**（1, 2, 3）、**中文数字**（一、二、十、百、千、零）以及常见的**罗马数字**（I, II, III）。
	    * **强制行长防误判机制 (Line-Length Threshold)**：为避免误杀正文，增加硬性约束条件：只有当命中规则的该行**总字符数 ≤ 35 个字符**（阈值可配）时，才被确认为合法章节标题。彻底规避类似“*第一章的内容他早就背得滚瓜烂熟了*”这种普通长句导致的目录解析灾难。
	
	* **自定义匹配：智能引导模式 (Smart Guided Mode)**：
	    针对不规则的异形章节名（如 `☆卷二 chap124 陨落☆`），提供无需编写代码的“向导式正则生成器”：
	    * **特征样本捕获**：在 UI 界面唤出专用弹窗，要求用户直接从源文件中复制并粘贴 1~2 个典型的异形章节行作为“训练样本”。
	    * **启发式正则自动推导 (Auto-Regex Deduction)**：
	        * **锚点定位算法**：后端接收样本后，自动扫描样本字符串，将“变化的部分”（如数字 `124` 或 `一百二十`）剥离出来，自动替换为正则的捕获组 `(\d+)` 或 `([一二三...零]+)`。
	        * **前缀/后缀转义**：将数字前后的固定字符（如 `chap`、`☆`）自动进行正则特殊符号转义，并拼装成完整的锚定行首的正则表达式（例如推导出：`^☆卷二\schap\s*(\d+).*?$`）。
	    * **沙箱即时校验与微调 (Real-time Validation Sandbox)**：
	        * **实时目录树抽取**：在弹窗右侧或下方提供一个预览面板。当正则生成后（或用户手动修改后），系统立即对预览切片（或后台全量文本）执行异步匹配，并将命中提取出的**纯标题以列表树的形式展示出来**。
	        * **可视化纠错**：用户可以通过查看抽取出的列表是否混入了奇怪的长句子，来直观判断推导出的正则是否精准。支持用户直接在输入框中修改系统推导出的正则表达式，下方列表列表将防抖（Debounce）实时更新。

	*   **章节序号重排**：支持对识别出的章节进行重新顺序编号，修复序号跳变。
		* 章节输出结构
			Chapter
			{
			  index: int
			  title: str
			  line_number: int
			}

* **9.断句缝合**
	* **行尾状态特征识别 (Tail Feature Detection)**：
	    * **非终结符判定**：检测第 $N$ 行的最后一个非空字符。如果该字符是逗号（`，`）、顿号（`、`）、无标点汉字、或是英文字母，且**不属于**标准的句末终结符（如 `。` `！` `？` `”` `」` `…` `~`），则将该行标记为“高疑似断句行”。
	* **行首状态特征校验 (Head Feature Detection)**：
	    * **首字符校验**：检测第 $N+1$ 行的第一个非空字符。如果它以标点符号开头（尤其是 `，` `。` `”` 等原本应该连在上一行的符号），或者以非缩进的普通文字开头，则进一步确认缝合条件。
	    * **对话防误伤豁免**：若第 $N+1$ 行以左引号（`“` 或 `「`）开头，无论上一行状态如何，**绝对禁止**缝合，以保护角色对话的独立换行。
	* **缝合执行与安全边界 (Execution & Constraints)**：
	    * **长度阈值过滤**：为了防止将短标题、诗歌或列举项（如“第一卷”、“主角属性：”）错误合并，可设定一个**最短行宽阈值**（如：只有当第 $N$ 行字符数大于 15 个字时，才触发缝合检测）。
	    * **执行缝合**：满足上述所有安全条件后，移除第 $N$ 行与第 $N+1$ 行之间的换行符 `\n`。对于中文，直接首尾相连；若缝合处两侧均为英文字母，则自动补充一个半角空格 `\u0020` 以防单词粘连。
* 去广告 / 去网站信息（只做ui，后端先不做）：
	*   内置常见小说网站广告正则库，并支持多维度自定义过滤（批量输入、正则/字面量切换、预览高亮）。（规则存储在本地 JSON）


# 3.用户动作-保存清理后的txt文件
网页左边功能区下方是[保存文件]按钮
用户点击[保存文件]按钮，程序把内存的文件保存到本地。和上传的txt同一路径，但加上后缀 { -已处理}。不提供覆盖原文件功能。log提示“已保存+文件的绝对地址”（例如：已保存：C://XXX//XXX//XXX.TXT）
例如用户上传的文件名是“盗墓笔记.txt”，保存的文件名是“盗墓笔记-已处理.txt”
成功保存后ui弹出提示
# 4.用户可选看log
网页左边最下方是一个可折叠（平时默认折叠）的log信息框，展示”时间+log信息“
info级别：用户的每一步操作（便于理解的人类语言）
warn级别：警告信息（便于理解的人类语言）
error级别：程序报错信息（类似try except捕获的信息，其实是给程序员看的，用户看不懂也没事）

# 5.技术路线与框架约束

*   **前端**：Vue3 + Diff-Viewer 组件。
	* 构建工具：Vite
	* UI组件库：Tailwind CSS/Ant Design /element Plus
	* Diff：diff2html
	* 状态管理：Pinia
*   **后端**：
	* - Python 3.11+
	- Web 框架：FastAPI
	- 文本处理：标准库 + `regex`（替代 `re`）
	- 编码检测：`charset-normalizer` 
	- HTML 清理：内置 `html` 模块 + 高效正则
	- SSE：FastAPI StreamingResponse
*  **数据存储**
	- 本地Json
	- 不使用数据库
* **运行方式**
	- 单机web工具
	- 不需要用户登录系统
* **文件处理方式**
	* 所有txt文件仅在本地临时目录处理
	* 不进行云存储
*   **工程化规范**：
    *   **目录结构**：前端代码 (`/frontend`) 与后端代码 (`/backend`) 目录分离。
    *   **环境管理**：提供标准的 `requirements.txt` 维护依赖，支持在 IDE 中快速拉起虚拟环境。
    *   **版本控制**：配置标准的 `.gitignore` 忽略二进制文件及临时日志。
*   **本地服务增强**：
    *   **打包方案**：使用 PyInstaller 打包为 EXE。
    *   **自动开启**：集成 `webbrowser` 实现运行即打开网页。
    *   **端口管理**：若默认端口 (8000) 被占用，系统应能自动检测并切换到可用端口，并给出相应提示。

## 6.目录结构
* **目录结构**
	/27txt-formatter
	├── /frontend          # Vite + Vue3/React
	│   ├── /src
	│   │   ├── /components
	│   │   ├── /store     # RuleConfig 状态管理
	│   │   └── /api       # Axios 封装
	├── /backend           # Python FastAPI
	│   ├── main.py        # 启动入口 & 端口自适应逻辑
	│   ├── core/          # 核心清洗算法 (正则、编码转换)
	│   ├── models/        # Pydantic Schemas
	│   └── logs/          # 滚动日志目录
	└── requirements.txt

* **输入格式**
	上传文件：
	- 单个 .txt
	- 最大 50MB
	编码：
	- 任意常见编码
	字符集：
	- Unicode

* **输出格式**
	输出文件：
	原来文件名-已清洗.txt
	编码：
	UTF-8 (无 BOM)
	换行：
	\n

- **核心 API 路由表**：
    - `POST /api/upload`：接收文件，返回 `task_id`。
    - `GET /api/stream-progress/{task_id}`：SSE 进度通道。
    - `POST /api/process/{task_id}`：提交 `RuleConfig` 开始清洗。
    - `GET /api/download/{task_id}`：获取最终文件。

## 7.本项目不包含的功能
- EPUB 生成
- TXT 转 PDF
- OCR
- GUI 桌面版
- 数据库
- 用户账号系统

# 8.vibe coding分步执行路径

  第一阶段：项目骨架与基础环境 (The Skeleton)
   1. 环境初始化：
       * 搭建 /backend 目录，安装 FastAPI、uvicorn、regex 等基础依赖。
       * 搭建 /frontend 目录，使用 Vite 初始化 Vue3 项目，引入 Tailwind CSS 。
   2. 布局实现：
       * 按照 2/3（功能区）和 1/3（预览区）的比例完成基础网页布局。
       * 实现左侧最下方的折叠日志框（Log Panel）基础 UI。


  第二阶段：文件生命周期管理 (Core Pipeline)
   3. 上传与初步处理：
       * 实现前端上传按钮与后端 /api/upload 接口。
       * 后端逻辑：自动探测编码、转码 UTF-8、去除 BOM 头。
       * 反馈：在页面实时显示“已转为 utf-8”、“已去除 BOM”等提示文字。
   4. 预览机制：
       * 后端提取转码后文件的前 500 行，返回给前端展示在右侧预览区。
   5. 保存/下载：
       * 实现 /api/download 接口，确保处理后的文件能以 文件名-已处理.txt 格式下载到本地。


  第三阶段：基础清理功能 (Atomic Cleaning)
   6. 最高优先级处理：
       * 实现“换行符归一化”逻辑（\r\n -> \n），这是后续正则处理的基石。
   7. 结构化清理（简单正则）：
       * 空行压缩：实现连续空行的折叠逻辑。
       * 段落清洗：实现行首尾去杂（Trim）及可选的强制缩进。
       * 非法字符过滤：剔除 C0/C1 控制字符及不可见零宽字符。
       *  ┌────────────┬──────┬─────────────────┬────────────┬────────────┐
  │ 编码 (Hex) │ 缩写 │ 名称            │ 说明       │ 本项目处理 │
  ├────────────┼──────┼─────────────────┼────────────┼────────────┤
  │ U+0000     │ NUL  │ Null            │ 空字符     │ 剔除       │
  │ U+0007     │ BEL  │ Bell            │ 响铃       │ 剔除       │
  │ U+0008     │ BS   │ Backspace       │ 退格       │ 剔除       │
  │ U+0009     │ HT   │ Horizontal Tab  │ 水平制表符 │ 保留 (\t)  │
  │ U+000A     │ LF   │ Line Feed       │ 换行符     │ 保留 (\n)  │
  │ U+000C     │ FF   │ Form Feed       │ 换页符     │ 剔除       │
  │ U+000D     │ CR   │ Carriage Return │ 回车符     │ 保留 (\r)  │
  │ U+001A     │ SUB  │ Substitute      │ 替换 (EOF) │ 剔除       │
  │ U+001B     │ ESC  │ Escape          │ 退出键     │ 剔除       │
  │ U+007F     │ DEL  │ Delete          │ 删除       │ 剔除       │
  └────────────┴──────┴─────────────────┴────────────┴────────────┘



  第四阶段：复杂文本逻辑 (Advanced Logic)
   8. 全半角转换：
       * 实现字母/数字转半角、标点符号转全角的精细化控制逻辑。
   9. HTML/XML 清理：
       * 实现 HTML 标签剔除与实体反转义（如 &nbsp; 还原）。
   10. 断句缝合：
       * 实现基于行尾特征检测（非终结符）和行首校验（对话保护）的自动缝合算法。


  第五阶段：深度交互与智能识别 (Interactive Features)
   11. 手动过滤乱码 (Modal)：
       * 开发弹窗 UI，实现用户粘贴样本 -> Unicode 解析 -> 字符按钮化 -> 点击加入黑名单的交互流程。
   12. 章节识别核心：
       * 实现内置正则库（中文数字/阿拉伯数字/罗马数字）。
       * 引入“行长防误判机制”（≤35个字符）。
   13. 智能引导模式 (Smart Guided Mode)：
       * 开发向导弹窗，实现用户提供样本 -> 后端自动推导正则（锚点算法）-> 实时目录树预览。


  第六阶段：工程化与本地增强 (Productization)
   14. 状态与通信优化：
       * 使用 Pinia 管理 RuleConfig 状态，确保“开始处理”按钮能准确传递勾选的功能参数。
       * 集成 SSE (Server-Sent Events) 实现处理进度的实时通知。
   15. 本地服务优化：
       * 实现后端启动时的端口自适应检测逻辑。
       * 集成 webbrowser 实现后端运行后自动弹出前端网页。
       * （可选）使用 PyInstaller 尝试打包测试。

# 9.修复bug、其他修改项

### 已完成
【2026.3.9】 导入txt文件后，再点击“章节识别与序号重排”，导入文件的按钮中的文字会出现异常错位。
- 【2026.3.9】新增功能：右边文本预览区[按章节读取]，初始状态锁定。
- 【2026.3.9】新增功能：点击“章节识别与序号重排”，推导出正则表达式之后。右边文本预览区的[按章节读取]解锁，点击后是一个包括所有章节目录的下拉框（支持用户滑动查看），用户点击下拉框的具体章节后，文本预览区刷新显示对应章节。
- 【2026.3.9】新增功能：上传txt文件后，自动识别章节的正则表达式。识别成功后，右边文本预览区的[按章节读取]解锁，用户可以选择网页具体展示哪一章。

为了方便阅读，正则表达式中的 `[数字]` 代表一个**宏（Macro）或组合正则组**，在实际代码中，你需要将其替换为能同时匹配阿拉伯数字和中文数字的完整正则段（例如：`[一二三四五六七八九十百千万零〇\d]+`）。另外，正则中的 `|` 符号在表格中使用了 `\|` 进行转义以防表格破裂，实际使用时请去掉反斜杠。

| **分类流派**                                   | **细分格式**      | **样本**                                              | **基础正则**                                                              |
| ------------------------------------------ | ------------- | --------------------------------------------------- | --------------------------------------------------------------------- |
| **1. 传统标准型**<br><br>  <br><br>_(覆盖率 ~70%)_ | 标准章/节/回       | `第1章 陨落的天才`<br><br>  <br><br>`第一百二十回 借东风`           | `^第\s*[数字]+\s*[章节回]\s*(.*)$`                                          |
|                                            | 大卷/大部         | `第一卷 斗之气`<br><br>  <br><br>`第七篇 尾声`                 | `^第\s*[数字]+\s*[卷部篇折]\s*(.*)$`                                         |
|                                            | VIP 章节前缀      | `正文 第一百章`<br><br>  <br><br>`VIP卷 第5章`               | `^(?:正文\|VIP卷\|VIP章节)?\s*第\s*[数字]+\s*[章节回]\s*(.*)$`                   |
| **2. 极简西方型**<br><br>  <br><br>_(覆盖率 ~15%)_ | 纯数字加点/顿号      | `1. 序言`<br><br>  <br><br>`001、 初到异界`                | `^\s*\d+(?:\.\d+)?\s*[\.、]\s*(.*)$`                                   |
|                                            | 英文 Chapter 变体 | `Chapter 1: The Beginning`<br><br>  <br><br>`Vol.2` | `^\s*(?:Chapter\|Ch\|Part\|Vol\|Episode)\.?\s*[数字]\s*[:\-\s]\s*(.*)$` |
|                                            | 全角数字直排        | `００１ 穿越`<br><br>  <br><br>`１２４ 巅峰对决`                | `^\s*[０-９]+\s+(.*)$`                                                  |
| **3. 符号装饰型**<br><br>  <br><br>_(覆盖率 ~10%)_ | 实心/空心方括号      | `【第一章】 觉醒`<br><br>  <br><br>`[第12章] 逃亡`             | `^\s*[【\[]\s*(?:第\s*)?[数字]\s*[章节回]?\s*[】\]]\s*(.*)$`                  |
|                                            | 星星/特殊符号夹心     | `☆第1章☆`<br><br>  <br><br>`★第一百回★`                   | `^\s*[☆★◆◇■□]\s*(?:第\s*)?[数字]\s*[章节回卷]\s*[☆★◆◇■□]\s*(.*)$`            |
|                                            | 波浪线/横线包围      | `~~~第5章~~~`<br><br>  <br><br>`--- 第一百节 ---`         | `^\s*[~\-]{2,}\s*第\s*[数字]\s*[章节回]\s*[~\-]{2,}\s*(.*)$`                |
| **4. 独立词汇型**<br><br>  <br><br>_(覆盖率 ~4%)_  | 序言与开篇         | `序`<br><br>  <br><br>`引子`<br><br>  <br><br>`楔子`     | `^\s*(?:序\|序章\|序言\|引子\|楔子\|前言)\s*(.*)$`                               |
|                                            | 结尾与补充         | `尾声`<br><br>  <br><br>`番外`<br><br>  <br><br>`大结局`   | `^\s*(?:尾声\|终章\|后记\|番外(?:篇)?\|附录\|大结局\|完本感言)\s*(.*)$`                 |
| **5. 危险兜底型**<br><br>  <br><br>_(极易误判)_     | 纯数字序列盲猜       | `01`<br><br>  <br><br>`124`<br><br>  <br><br>`003`  | `^\s*\d+\s*$`                                                         |
- **预扫描抽样**：用户上传了一本《斗破苍穹.txt》，系统不扫全书，只随机抽取前 `1000` 行。
    
- **矩阵轰炸**：系统把上述的 20 几个正则，像过筛子一样，对这 1000 行逐行扫描。
    
- **记录命中数**：“规则1（标准章）”命中了 15 次，“规则10（特殊词）”命中了 1 次，“规则5（英文）”命中了 0 次。
    
- **数字递增校验（防伪认证）**：系统单独拿出命中“规则1”的那 15 句话，把里面的数字提取出来，发现是 `[1, 2, 3, 4, 5... 15]`。这是一个完美的等差数列！
    
- **胜出宣告**：系统在后台抛弃其他所有规则，**锁定“规则1（标准章）”为该书的唯一章节正则**，并自动应用到后续全文的清洗中。
【2026.3.9】修改ui，上传文件后，不要默认勾选“章节识别与序号重排”
【2026.3.9】上传文件后，如果能成功执行自动按章节读取功能，就在内存中保存对应的章节正则规范。勾选”自动处理“的栏目后点击“开始处理”按钮（虽然此时用户没有选择手动处理的“章节识别与序号重排”），刷新文档时按照之前保存的章节正则规范，在右边文本预览区解锁“按章节读取”功能
【2026.3.9】文本预览区会记录用户上一次选择过的章节，用户修改处理条件后，预览区会展示上次所选的章节
【2026.3.9】新增功能：折叠左边用户操作区（类似gemini网页左边的“展开菜单”、“收起菜单”）
【2026.3.9】bug修复：用户在文本预览区操作滑块到下方，用户选择了新的章节，滑块应该自动重置到上方，展示新章节最开始的内容
### 未完成

-END-